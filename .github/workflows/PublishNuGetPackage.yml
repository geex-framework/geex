name: Build and Publish NuGet Package

on:
  push:
    branches:
       - '*'
       
jobs:
  build-and-publish-nuget-package:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup .NET Core SDK
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 7.0.203

      - name: Restore dependencies
        run: dotnet restore ./modules/Geex.Platform/server/Geex.Bms.sln
        
      - name: Build projects
        run: dotnet build --configuration Release ./modules/Geex.Platform/server/Geex.Bms.sln

      - name: Generate NuGet packages
        run: echo "Tag name:" + ${GITHUB_REF#refs/tags/v}
        
      - name: Generate NuGet packages
        run: dotnet pack ./modules/Geex.Casbin/src/NetCasbin.csproj --configuration Release --no-build --output ./nupkg  /p:PackageVersion="" + ${GITHUB_REF#refs/tags/v}
      # - name: Publish packages to NuGet.org
      #   uses: nuget/publish@1.0.0
      #   with:
      #     nuget-api-key: ${{ secrets.NUGET_API_KEY }}
      #     nuget-package-dirs: './nupkg/*.nupkg'





